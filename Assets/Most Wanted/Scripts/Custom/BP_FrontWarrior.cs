using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BP_FrontWarrior : BoardPiece
{/*
    public override bool IsValidMove(Cell targetCell,Cell currentCell)
    {
        if (!base.IsValidMove(targetCell,currentCell)) return false;

        if (Mathf.Abs(currentCell.x - targetCell.x) > 1) return false;
        if (Mathf.Abs(currentCell.y - targetCell.y) > 1) return false;
        
        return true;
    }*/
}
